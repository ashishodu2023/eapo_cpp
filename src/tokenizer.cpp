#include "../header/tokenizer.hpp"
#ifdef USE_TOKENIZERS
#include <stdexcept>

// (Implementation is inline in the header; no cpp code needed unless additional helpers are required.)
#else
// If tokenizers disabled, no implementation
#endif
